# `homer3_betas_plus_combined_sfv_data_inner_join.csv` — column descriptions

This document describes the columns in `data/tabular/generated_data/homer3_betas_plus_combined_sfv_data_inner_join.csv`.

## Dataset overview

- **Row unit:** one row per participant (`subject_id`).
- **Design context:** within-subjects 2×2 factorial with four conditions:
  - `Cond01` = Short-Form Education
  - `Cond02` = Short-Form Entertainment
  - `Cond03` = Long-Form Entertainment
  - `Cond04` = Long-Form Education
  (Condition mapping per `ANALYSIS_SPEC.md`.)
- **How this file is produced (high level):**
  1) Tabular behavioral + covariate features are merged into `data/tabular/generated_data/combined_sfv_data.csv` by `generate_combined_data.py`.
  2) Externally-produced Homer3 FIR basis weights in `data/tabular/homer3_glm_betas_wide_fir_pca.csv` are collapsed to baseline-corrected task-window AUC betas in `data/tabular/generated_data/homer3_glm_betas_wide_auc.csv` by `collapse_homer_fir_to_auc.py`.
  3) The raw derived AUC table is screened between subjects within each exact `channel x condition x chromophore` column by `mask_homer_auc_between_subject_outliers.py`, which writes `data/tabular/generated_data/homer3_glm_betas_wide_auc_outliers_masked.csv`.
  4) The outlier-masked AUC table is inner-joined to `combined_sfv_data.csv` by `merge_homer3_betas_with_combined_data.R` (default output path is this file).
  5) Because the merge is an **inner join**, participants not present in both inputs are dropped from this merged table.
  6) The raw derived AUC table also carries a sidecar provenance file `data/tabular/homer3_glm_betas_wide_auc.provenance.json` that must match the raw FIR export and settings JSON used to generate it.
  7) The outlier-masking stage also writes `data/results/homer_auc_outlier_audit.csv` and `data/results/homer_auc_outlier_summary.json` for QC traceability.

## Critical missingness / pruned-channel note (fNIRS betas)

Per repo policy for Homer3 beta imports:
- The raw FIR basis-weight table can contain **`0` and/or `NaN` values that stand in for pruned channels**.
- The raw derived AUC beta table carries those pruned channels forward as **`NaN`**.
- The outlier-masked AUC beta table used for merging carries both pruned channels and between-subject `mean +/- 3 SD` censored values as **`NaN`**.
- Do **not** interpret these as “true zero activation” and do **not** silently impute them; treat them as **missing/pruned** in downstream analyses.

## Column glossary (non-fNIRS columns)

### Identifiers

- `subject_id`
  - Participant identifier used to merge across tabular sources and Homer betas.
  - During merges/analyses, IDs are typically normalized by extracting digits (e.g., `"sub_0051"` → `51`).

- `homer_subject`
  - The subject identifier as provided in the Homer AUC table (e.g., `sub_0001`), preserved for traceability after merging.

### Engagement (ratings during the fNIRS session)

These columns come from `data/tabular/generated_data/engagement_data_processed.csv`, derived from the long-format table
`demographic/combined_engagement_data.csv` (columns: `subject_id`, `Trigger`, `Category`, `Rating`).

All engagement features are computed **per participant** as means of the raw `Rating` values:

- `lf_education_engagement`
  - Mean `Rating` restricted to `Category == "Long-Form Educational"`.

- `lf_entertainment_engagement`
  - Mean `Rating` restricted to `Category == "Long-Form Entertainment"`.

- `sf_education_engagement`
  - Mean `Rating` restricted to `Category == "Short-Form Education"`.

- `sf_entertainment_engagement`
  - Mean `Rating` restricted to `Category == "Short-Form Entertainment"`.

- `long_form_engagement`
  - Mean `Rating` across all rows whose `Category` contains `"Long-Form"` (i.e., pooled long-form education + long-form entertainment),
    **weighted by the number of ratings** contributing to each category (this is not necessarily the mean of the two condition means if counts differ).

- `short_form_engagement`
  - Mean `Rating` across all rows whose `Category` contains `"Short-Form"` (pooled short-form education + short-form entertainment),
    **weighted by the number of ratings** contributing to each category.

- `education_engagement`
  - Mean `Rating` across all rows whose `Category` contains `"Educ"` (education/educational),
    **weighted by the number of ratings** contributing to each category.

- `entertainment_engagement`
  - Mean `Rating` across all rows whose `Category` contains `"Entertain"` (entertainment),
    **weighted by the number of ratings** contributing to each category.

### Demographics + SFV usage + questionnaires (covariates)

These columns come from `data/tabular/generated_data/socio_demographic_data_processed.csv`, generated by `process_sociodemographic.py`
from the Qualtrics export `qualtrics/final_SF_demographic_data.csv`.

- `age`
  - Age in years (numeric).

- `pd_status`
  - Binary indicator from a Yes/No item (encoded: `No = 0`, `Yes = 1`).

- `sfv_frequency`
  - Ordinal encoding of self-reported short-form video frequency (higher = more frequent), mapped to integers `0`–`3` by `process_sociodemographic.py`.

- `sfv_daily_duration`
  - Ordinal encoding of self-reported daily short-form video duration (higher = longer duration), mapped to integers `0`–`3` by `process_sociodemographic.py`.
  - “Other (please specify)” responses are encoded as missing (`NaN`) by default (see `process_sociodemographic.py`).

- `phq_total`
  - Composite sum score for the intentionally administered PHQ-8 depression questionnaire.
    In the current Qualtrics export, those 8 depression items still carry the label `"PHQ-9"`, so `process_sociodemographic.py` selects the 8 PHQ-labeled items and scores them as PHQ-8.
  - Scoring uses the `0`–`3` response encoding in `process_sociodemographic.py` (`Not at all`→0, …, `Nearly every day`→3),
    and requires at least 75% of items answered to compute a total.
  - Instrument reference: PHQ-8 (Kroenke et al., 2009; see `CITATIONS.md`).

- `gad_total`
  - Composite sum score over the Qualtrics items labeled `"GAD"` (anxiety symptom items).
  - Scoring uses the `0`–`3` response encoding in `process_sociodemographic.py` (`Not at all`→0, …, `Nearly every day`→3),
    and requires at least 75% of items answered to compute a total.
  - Instrument reference: GAD-7 (Spitzer et al., 2006; see `CITATIONS.md`).

- `asrs_total`
  - Composite sum score over the Qualtrics items labeled `"ASRS"` (adult ADHD symptom items).
  - Scoring uses the `0`–`4` response encoding in `process_sociodemographic.py` (`Never`→0, …, `Very often`→4),
    and requires at least 75% of items answered to compute a total.
  - Instrument reference: WHO ASRS (Kessler et al., 2005; see `CITATIONS.md`).

- `yang_pu_total`
  - Composite total over Qualtrics items labeled `"Yang Problematic Use"` (Likert-type).

- `yang_mot_total`
  - Composite total over Qualtrics items labeled `"Yang Motivation"` (Likert-type).

### Recall / retention outcomes (post − pre)

These columns come from `data/tabular/generated_data/recall_assessment_score_diffs.csv`, generated by `demographic/process_recall_assessment.py`
from the pre- and post-task recall assessments plus the answer key.

Each value is:
- the participant’s **mean correctness** in that condition at **post-task**, minus mean correctness at **pre-task**,
- where correctness is scored per-item as 0/1 using conservative normalization and matching rules (see `demographic/process_recall_assessment.py`),
- and the mean is taken over items associated with that condition in `Recall_Assessment_Key.csv`.

- `diff_short_form_education`
  - Post − pre mean correctness for Short-form Education items.

- `diff_short_form_entertainment`
  - Post − pre mean correctness for Short-form Entertainment items.

- `diff_long_form_education`
  - Post − pre mean correctness for Long-form Education items.

- `diff_long_form_entertainment`
  - Post − pre mean correctness for Long-form Entertainment items.

## Column glossary (fNIRS beta columns)

### Pattern

Columns named like:

- `S##_D##_Cond##_HbO`
- `S##_D##_Cond##_HbR`

represent **single-value, baseline-corrected task-window AUC summaries** derived from the raw Homer3 FIR basis-weight export (`data/tabular/homer3_glm_betas_wide_fir_pca.csv`) and carried through the inner join via `data/tabular/generated_data/homer3_glm_betas_wide_auc_outliers_masked.csv`.

### What each part means

- `S##_D##`
  - The fNIRS **channel** defined by a source index (`S##`) and detector index (`D##`).

- `Cond01` … `Cond04`
  - The experimental condition for the beta (see the condition mapping in “Dataset overview”).

- `HbO` / `HbR`
  - Chromophore: oxygenated hemoglobin (`HbO`) or deoxygenated hemoglobin (`HbR`).

### Value interpretation

- Each beta cell is a single numeric coefficient for a given `(subject × channel × condition × chromophore)`.
- As noted above, `NaN` values indicate pruned channels in the derived AUC table and must be handled explicitly as missing.
